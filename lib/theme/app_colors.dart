import 'package:flutter/material.dart';

class AppColors {
  static const bgTop = Color.fromARGB(255, 0, 0, 0);
  static const bgMiddle = Color.fromARGB(255, 0, 0, 0);
  static const bgBottom = Color(0xFF000000);

  static const primaryYellow = Color(0xFFFFC107);
  static const primaryOrange = Color(0xFFFF9800);

  static const glassColor = Colors.white24;
}
